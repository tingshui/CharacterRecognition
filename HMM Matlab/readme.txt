This file is used to perform hmm experiment.

You should enter the input files, which you just created with characterReco.java in last step.

The inputs are listed from line 3 to line 6. It is straightforward to know the input files. This folder also contains all input files you need to run hmm.m.